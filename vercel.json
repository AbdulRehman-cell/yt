{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "comment": "Vercel is kept as a static/client-only fallback. For the full Express + MongoDB backend, deploy via Render using render.yaml. Vercel serverless functions would require restructuring server/index.js into api/ handlers."
}